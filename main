
#include "stdafx.h"
#include <vector>
using namespace std;
const double height=10;
const double width=10;
const int N=1000;
const double h=0.1;
const int widN = (int)(width / (2 * h) + 1);
const int heiN = int(height / (2 * h) + 1);



struct particle {
	double x, y, vx, vy, ax, ay, v;
};
struct grid {						//网格结构
	particle particle1[widN][heiN];
};

/*void move(vector<grid>&a, double dt) {

}  */

int main()
{

	vector<grid> net;
	net[0].[2][3].vx=10;
	cout << net[0].[2][3].vx;
    return 0;
}



